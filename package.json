{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://localhost:8080/toTomcat",
  "dependencies": {
    "bootstrap": "^4.1.3",
    "env-cmd": "^8.0.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-redux": "^6.0.0",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.1",
    "reactstrap": "^6.5.0",
    "redux": "^4.0.1",
    "redux-logger": "^3.0.6"
  },
  "scripts": {
    "//": [
      "NODE_ENV=development react-scripts start"
    ],
    "start": "react-scripts start",
    "start:dev": "cp env.development .env && npm run start",
    "start:prod": "cp env.production .env && npm run start",
    "build": "react-scripts build",
    "build:dev": "cp env.development .env && npm run build",
    "build:prod": "cp env.production .env && npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "aws s3 sync build/ s3://secondreactapp"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
